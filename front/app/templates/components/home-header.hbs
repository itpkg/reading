<nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#">{{site.subTitle}}</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <li>{{#link-to "index"}}{{t 'links.home'}}{{/link-to}}</li>
            <li>{{#link-to 'cms.articles' }}{{t 'links.articles'}}{{/link-to}}</li>
            <li>{{#link-to 'books' }}{{t 'links.books'}}{{/link-to}}</li>
            <li>{{#link-to 'video.items' }}{{t 'links.video.items'}}{{/link-to}}</li>
            <li>{{#link-to 'cms.articles.show' 'faq'}}{{t 'links.faq'}}{{/link-to}}</li>
            <li>{{#link-to 'cms.articles.show' 'about'}}{{t 'links.about'}}{{/link-to}}</li>
            <li>{{#link-to 'cms.articles.show' 'contact'}}{{t 'links.contact'}}{{/link-to}}</li>
            {{#if auth.current_user}}
              {{#bs-dropdown tagName="li"}}
                {{#bs-dropdown-toggle}}{{t 'users.welcome' name=auth.current_user.name }}
                  <span class="caret"></span>
                {{/bs-dropdown-toggle}}
                {{#bs-dropdown-menu}}
                  <li><a href="#" {{action "signOut"}}>{{t 'users.sign_out'}}</a></li>
                {{/bs-dropdown-menu}}
              {{/bs-dropdown}}
            {{else}}
              {{#bs-dropdown tagName="li"}}
                {{#bs-dropdown-toggle}}{{t 'users.sign_up_or_in'}} <span class="caret"></span>{{/bs-dropdown-toggle}}
                {{#bs-dropdown-menu}}
                  <li><a href="{{auth.oauth.google}}">{{t 'users.sign_in.google'}}</a></li>
                {{/bs-dropdown-menu}}
              {{/bs-dropdown}}
            {{/if}}
          </ul>
          <ul class="nav navbar-nav navbar-right">
            {{#each locales as |loc|}}
              <li><a href="#" {{action "setLocale" loc.id}}>{{loc.text}}</a></li>
            {{/each}}
          </ul>
          <form class="navbar-form navbar-right">
            <div class="form-group">
              <input type="text" placeholder={{t "placeholders.search"}} class="form-control">
            </div>
            <button type="submit" class="btn btn-success">{{t "buttons.search"}}</button>
          </form>
        </div>
      </div>
    </nav>
